


# System Tables -- A Tour {.dqlh}

**All namespaces and entities prefixed by `sys` do not
guarantee stability to a programmer over version changes.**

Nevertheless, the current version of delightql has
many candidates for promotion to `std` that there might
come a time when
